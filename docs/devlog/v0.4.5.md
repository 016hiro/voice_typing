# v0.4.5 — VAD 调参 + 幻觉过滤

> 流式质量补丁。两件事:
> 1. **VAD 默认值改为转写场景值** —— 之前用的是 speech-swift 给"低延迟流式响应"调的参数，对输入法这种"高质量切段"的场景太激进。`minSpeechDuration 0.25 → 0.3s`，`minSilenceDuration 0.10 → 0.7s`
> 2. **新增 `HallucinationFilter`** —— 拦截两种段输出：训练集尾巴（`谢谢观看` / `Thank you.`）+ prompt echo（Qwen 把传给它的 `热词：Rust、Python…` bias 原样吐回来）

UI 没动，权限没加。表面上 `streamingEnabled` toggle 行为不变，只是产出更干净。Info.plist 0.4.4 → 0.4.5，build 9 → 10。

## 背景

v0.4.2 ship 了 post-record 流式（opt-in experimental），v0.4.4 把胶囊 UI 收敛了一遍。日常使用中暴露两个真实故障模式：

- **训练尾巴 hallucination**：短录音或带噪音的段输出 `Thank you.` / `Yeah.` / `好的` / `谢谢观看`。这些是模型训练集尾巴 —— 中文那批数据里有大量 YouTube 字幕，英文有大量 podcast 收尾，音频太弱时模型 fall back 到这类高频终止符。

- **Prompt echo（Type B，意外发现）**：用户字典非空时，段输出会把整个 bias prompt 原样吐回来：
  ```
  字典: ["Rust", "Python", "Qwen3-ASR", "VAD", "E2E"]
  GlossaryBuilder.buildQwenContext 产出: "热词：Rust、Python、Qwen3-ASR、VAD、E2E。"
  Qwen 在噪音短输入下输出: "热词：Rust、Python、Qwen3-ASR、VAD、E2E。"
  ```
  Qwen3-ASR 的 `<|im_start|>system` 槽位本来是给 token bias 用的；当没有真实语音 anchor decoder 时，模型就把 prompt 自己当成 transcription 输出。

设计阶段的假设是这只在 Whisper 上发生。用户实测推翻了：Qwen 0.6B/1.7B 都中招。

## 调研：先 benchmark，后改值

直接拍参数容易偏，先建一个 benchmark 量化"切段策略"对结果的影响，再决定改什么。`make benchmark-vad`（`VT_BENCHMARK=1` 门控）扫现有 fixture（16 条 WAV，60s 以上的 2 条跳掉避免 MLX OOM，剩 14 条），每条跑 batch + N 个 streaming preset，metric 是 streaming 文本 vs batch 文本的字符级 Levenshtein 相似度。

特意不用 WER vs ground truth —— 我们关心的不是 backend 本身的准确率，而是**切段策略让 backend 退化了多少**。

### 第一轮：baseline / `(0.5, 0.7)` / `(0.5, 0.7)` + 0.2s padding

```
preset       sim%    latency    partials
baseline     99%     1037ms     3.3        (Silero 默认)
tuned        98%      813ms     1.4        (minSpeech 0.5, minSilence 0.7)
tuned+pad    97%      834ms     1.4        (+ 200ms 前后 padding)
```

发现：
- Latency -22%，partial 数减半 —— UX 改善实锤
- Padding 完全不值 —— sim 平或下降，latency 多 20ms
- **唯一回归**：`librispeech_1988` 把开头单词 `"Presently"` 整个丢了。`minSpeechDuration=0.5s` 把开头那个孤立的短词（<500ms）当噪音过滤了

### 第二轮：`(0.3, 0.7)` vs `(0.5, 0.7)` 头对头

```
preset       sim%    latency    partials
baseline     99%     1028ms     3.3
tuned-0.3    99%      850ms     1.4        ← 赢
tuned-0.5    98%      839ms     1.4
```

`(0.3, 0.7)` 把 `"Presently"` 收回来了，同时其他维度（latency / partial / 中文长录音 +5%）全保留 0.5 的成果。

剩两个 -2~3% 回归：
- `jfk_en_short`: -2% —— VAD 把 JFK 那段戏剧性停顿 ("ask not") 跨段 merge 掉了，`"ask not"` 这个词整个丢失。这个是 baseline 也错的（baseline 把它听成 `"Stop."`），不是新引入的
- `librispeech_1919_142785_short`: -3% —— `"Chaba"` 听成 `"chub. Yeah."`。**结尾那个 `"Yeah."` 就是 Type A 训练尾巴**，正好印证了 filter 的需求

### 第三轮：production = `(0.3, 0.7)` + filter

```
preset        sim%    latency    partials
baseline       99%    1003ms     3.3
production     99%     849ms     1.4        ← shipping
```

`librispeech_1919` 回血到 99%，证实 `"Yeah."` 被 filter 拦掉了。平均 99%，与 baseline 持平。剩下 jfk 的 -2% 是 VAD 边界精度问题（不是尾巴/echo），filter 解不了，留给 v0.5 的 live-mic 配 pre-padding 时再处理。

## 变更

### 1. `StreamingTuning.production`

```swift
// Sources/VoiceTyping/ASR/QwenASRRecognizer.swift
public extension QwenASRRecognizer {
    struct StreamingTuning: Sendable {
        public let minSpeechDuration: Float?    // nil = 用上游默认
        public let minSilenceDuration: Float?
        public let paddingSeconds: Float
        public let maxSegmentDuration: Float

        /// 上游 Silero 默认值 (0.25s, 0.10s)。保留给 benchmark 做 baseline。
        public static let `default` = StreamingTuning()

        /// v0.4.5 shipping 值: (0.3s, 0.7s, 无 padding)
        public static let production = StreamingTuning(
            minSpeechDuration: 0.3,
            minSilenceDuration: 0.7,
            paddingSeconds: 0,
            maxSegmentDuration: 10.0
        )
    }
}
```

`AppDelegate.runASR` 显式传 `tuning: .production`。`.default` overload 保留，给 benchmark 用 + 给将来需要 opt-out 的 caller。

### 2. `HallucinationFilter`

新文件 `Sources/VoiceTyping/ASR/HallucinationFilter.swift`，两层：

**Layer 1 — 静态黑名单**（Type A）。约 30 个条目，覆盖观察到的 Qwen / Whisper 训练集尾巴：

```swift
"谢谢观看", "明镜与点点栏目", "字幕由 Amara.org 社区提供", ...
"好的", "嗯", "对", "啊", "谢谢", ...
"Thank you.", "Thanks for watching.", "Yeah.", "OK.", "Mhm.", ...
"♪", "(music)", "(applause)", ...
```

匹配在 normalized form 上做（lowercase + 去空白 + 去 Unicode 标点），所以 `"Yeah."` / `"yeah"` / `"YEAH!"` 都命中同一个条目。**只对整段匹配**，不做 substring —— `"Thanks for the help"` 是真实口述，必须保留，即使 `"Thanks."` 在黑名单里。

**Layer 2 — Prompt echo 检测**（Type B），三个子信号：

1. **快速正则**：段以 `热词:` 或 `热词：` 开头 → 无条件丢。没人说话会以"hotwords colon"开头，纯 echo 信号。

2. **互相 substring 包含**：把段和 ctx 都 normalize 后，`normalizedCtx.contains(normalized) || normalized.contains(normalizedCtx)` → 丢。覆盖英文 bare-comma 形式 (`"Rust, Python, Qwen3-ASR, VAD, E2E"`)、Qwen 把中文 echo 改了几个字符的情况、以及部分前缀 echo（被 maxTokens 截断的）。

3. **短 ctx guard**：normalized ctx 字符数 `< 12` 直接跳过 Layer 2。1 个词的字典（如 `["Linus"]`）normalize 出来 5 字符，任何包含 "Linus" 的段都会误判。12 是经验值，substring overlap 在这之下基本是巧合不是 echo。

挂在 `runStreaming.transcribeSegment` 里 Qwen 调用之后、accumulated 之前：

```swift
if HallucinationFilter.isLikelyHallucination(segment: trimmed, context: ctx) {
    Log.dev(Log.asr, "Hallucination filtered: \(trimmed)")
    return  // ← 段被丢，不 yield，不进 accumulated
}
```

被过滤的段发一行 `Log.dev`，开发者模式下能看见拦了什么。如果以后日志噪音太大再调；v0.4.5 期间这个可见度反而有用。

### 3. Benchmark + Makefile

`Tests/VoiceTypingTests/E2E/E2EVADTuningBenchmarkTests.swift` 是新测试，`make benchmark-vad` 是入口。`VT_BENCHMARK=1` 门控，所以日常 `make test-e2e` 不付那 5 分钟代价。默认跳 > 60s 的 fixture（用 `VT_BENCHMARK_MAX_DUR` 覆盖）—— 97s 的 LibriSpeech 长 fixture 在 Qwen 1.7B 上跑到第 4 个 transcribe 就把 MLX cache 撑爆。

现在带两个 preset（baseline / production），兼任 release-gate。要新增候选 preset，文件里加一行就行。

### 4. 单元测试

`Tests/VoiceTypingTests/Unit/HallucinationFilterTests.swift` —— 11 个测试，~50ms：

- Layer 1 每个条目能拦截
- 真实口述里包含黑名单子串（`"Thanks for the help"`、`"嗯，这个方案可以试试"`）必须保留
- `热词:` 前缀无论有无 ctx 都拦
- 完整 / 部分 / 改了几字的 echo 都拦
- 真实口述里包含字典词（`"我用 Rust 写了一个小工具"`）必须保留
- 1 词字典不会让任何包含该词的段误判
- nil ctx 干净跳过 Layer 2
- 空 / 纯空白段不拦

## 明确不做

- **Padding 200ms** —— 两轮 benchmark 都证明无收益，砍
- **Layer 2 用 Levenshtein** —— substring 包含够用了。如果以后看到 Qwen 把 echo 改字符（如 `"热辞：…"` 错别字）超出 substring，再加 Levenshtein
- **黑名单暴露到 Settings** —— 系统级调优，开了就要支持"我加了 OK 进黑名单结果什么都转不出来了"。硬编码
- **Filter 同时挂到 batch 路径** —— batch 基本不出 Type A（音频长，decoder 有 anchor），完全不出 Type B（没有段级 ctx echo loop）。如果用户报上来再加
- **Live-mic / auto-stop / streaming mode picker** —— 留 v0.5。v0.4.5 是让现存流式用户立刻体验改善的最小补丁

## 快速自测

1. `make build && make install`
2. `open /Applications/VoiceTyping.app`
3. Settings → **Models** → 开 **Streaming (experimental)**，refinement = **Off**
4. Settings → **Dictionary** → 加 3-5 个词（如 `Rust`、`Python`、`Qwen3-ASR`、`VAD`）
5. Settings → **Advanced** → 开 **Developer logging**
6. 终端开:
   ```bash
   log stream --predicate 'subsystem == "com.voicetyping.app"' \
              --style compact --level info | grep -iE "hallucin|latency"
   ```
7. 按 Fn 短暂、不说话 → 松开。应该看到要么没有 transcript，要么 log 里有 `Hallucination filtered: ...` 一行（而不是把 `好的` / `Yeah.` / `热词：Rust、Python…` 粘进当前焦点 app）
8. 按 Fn 正常说 ~10s → 松开。胶囊 `TRANSCRIBING` → 文本进 app。`latency ... mode=off llm_ms=-1 asr_ms=<num>` 应该比 v0.4.4 同长度的低

真实自测（最有用）：
- 重现你之前出 `"热词：…"` 的那段噪音短输入 —— 现在应该静默吃掉
- 长口述 (30-60s) 应该 commit 更快（段更少 → Qwen 串行调用次数更少）

## Benchmarking 用法

```bash
make benchmark-vad
```

末尾会打 recap 表，长这样：

```
average           │ baseline   99% / 1003ms / 3.3 │ production  99% /  849ms / 1.4
```

production 行的 sim% 必须不低于 baseline 3% 以内。低于这个就是 `StreamingTuning.production` 或 `HallucinationFilter` 改坏了 —— 用 recap 下面的 per-fixture delta 对照 bisect。

## 文件变动

```
Resources/Info.plist                                |   4 +-    0.4.4 → 0.4.5, build 9 → 10

Sources/VoiceTyping/
  ASR/HallucinationFilter.swift                     | 119 +++   新文件
  ASR/QwenASRRecognizer.swift                       |  50 ++    + StreamingTuning, padding,
                                                                + filter 调用点
  AppDelegate.swift                                 |   2 +-    runASR 显式 .production

Tests/VoiceTypingTests/
  Unit/HallucinationFilterTests.swift               | 156 +++   新文件 (11 tests)
  E2E/E2EVADTuningBenchmarkTests.swift              | 240 +++   新文件 (gated benchmark)

Makefile                                            |  18 ++    + benchmark-vad target

docs/
  devlog/v0.4.5.md                                  | (本文)
  architecture.md                                   |   1 +-    header 引用更新
  todo/backlog.md                                   |   1 +-    footer 引用更新
```

## 参考

- v0.4.2 流式 devlog（背景）：[v0.4.2.md](v0.4.2.md)
- v0.4.4 收敛 devlog（直接前置）：[v0.4.4.md](v0.4.4.md)
- VAD 上游默认值：`.build/checkouts/speech-swift/Sources/SpeechVAD/Configuration.swift:74-91`
- Qwen3-ASR `<|im_start|>system` 槽位：`.build/checkouts/speech-swift/Sources/Qwen3ASR/Qwen3ASR.swift`
- `GlossaryBuilder.buildQwenContext`：`Sources/VoiceTyping/LLM/GlossaryBuilder.swift:44-56`
