# v0.5.2 — Live mode UI 公开 + 分析脚本前置 + dogfood polish

> 三件事：
> 1. **Live mode Settings UI 公开**：把 `liveStreamingEnabled` 从 UserDefaults-hidden 抬到 Settings → Models 一个明确的 toggle。**直接动机**：v0.5.1 dogfood 早期踩过"两个 Streaming 名字混淆"的坑（用户以为关掉 Settings 里的 "Streaming" 就关掉了 live mode，实际只关掉了 post-record VAD 流式）。同时把现有 "VAD-segmented transcription" 改名 "Post-record streaming" 让两个概念物理隔开。
> 2. **分析脚本前置**：在 dogfood 数据真正堆起来之前，先写好 `Scripts/analysis/` 下 4 个 Python stdlib 脚本，覆盖 v0.5.1.md #6 表格里纯 JSON 可解的 4 类分析。这样数据进来立刻能跑，不用临时 jq 拼。
> 3. **dogfood polish**：占位符，具体内容由真实使用反馈定。
>
> **状态**：planning。dogfood 数据收集中（用户自己判断"够"的标准，不再设硬性指标）。
>
> **预估范围**：~400 行 Swift（UI 公开 + 文案 + tests）+ ~300 行 Python（4 个分析脚本）+ 文档。1-2 周，主要时间花在 dogfood 等待。

## 设计决策

### Live mode UI 公开

**重命名 + 新增 toggle**，两者都在 Settings → Models 现有 SectionCard 内合并展示：

| 当前 | v0.5.2 |
|---|---|
| "Streaming (experimental)" SectionCard 一个 toggle "VAD-segmented transcription" | "Streaming" SectionCard 两个 toggle："Post-record streaming" + "Live transcription (experimental)" |
| `state.streamingEnabled` (key 不动) | 同上 |
| `state.liveStreamingEnabled` 隐藏 | 暴露给上面的 "Live transcription" toggle (key 不动，向后兼容) |

**两个 toggle 的关系**：
- 都依赖 `backend.isQwen` —— 非 Qwen backend 时两者都 disabled
- 互不依赖（用户可以单独开任一）。但 Live 优先级高于 Post-record（live 走 LiveTranscriber，bypass post-record 路径）—— 描述文案需说明
- "Post-record" 文案：录音结束后再分段转写，文字一次性出现
- "Live" 文案：录音过程中分段转写，文字逐段在焦点 app 里实时出现；不支持 refine

**UserDefaults 兼容**：两个 key (`streamingEnabled`, `liveStreamingEnabled`) 完全保留。老用户的设置在新 UI 里直接显示对应 toggle 状态。

### 分析脚本

按 `todo/v0.5.1.md` 决策 #7 "v0.5.1 不做工具" 的尾巴："工具留给 v0.5.2 或之后看实际需要再做"。现在拍板做，但范围收窄。

- **位置**：`Scripts/analysis/`（不污染 `Sources/`，不进 .app bundle，不参与 Swift build）
- **语言**：Python 3 stdlib only —— 不引 pandas / matplotlib / numpy。理由：
  - 用户运行不需要额外 `pip install`
  - 输出数据量小（典型一周 capture < 5 GB / < 1000 sessions），不需要 DataFrame
  - 第三方依赖会拖累"开箱即用"承诺
- **输入约定**：positional arg 指向 capture root 或单 session 目录。脚本自适应（如果是 root 就遍历所有 session；如果是单 session 就只算那一个）
- **输出约定**：文本 summary 到 stdout。脚本 `--csv` flag 可选输出 CSV 给后续 spreadsheet 处理（v0.5.2 暂不实现 CSV 选项，留接口）

**首批 4 个脚本**：

| 脚本 | 输入 | 输出 | 覆盖 v0.5.1.md #6 表 |
|---|---|---|---|
| `summary.py` | capture root | sessions / total audio / total segments / total injections / on-disk size + per-backend split | overview，新增 |
| `hallucination_review.py` | capture root | 所有 `filter: hallucinationFiltered` 段列出（rawText + 上下文 session ID + timestamp），可选 `--sample N` 随机抽样 | 第 3 行 "HallucinationFilter ±" |
| `live_drain.py` | capture root | live mode session 的"最后段 inject 完成时间 - Fn↑时间"分布，用 percentile / histogram 表示 | 第 5 行 "Live mode drain 时间分布" |
| `focus_drop.py` | capture root | `status: focusChanged` 数量 / 总 inject 数 / 比例，按 target bundleID 分组 | 第 6 行 "Focus-change drop 频率" |

**不进首批**（v0.5.1.md #6 表里的另两类，需要 Swift 跑模型）：

- **VAD config replay**（audio + 不同 `StreamingTuning` → 看分段差异）：需要 Swift LiveTranscriber 实例 + Silero VAD 模型加载。要做就单独成 Swift CLI binary，不该用 Python 弄
- **跨 backend ASR 对比**（同 audio → 三 backend → diff transcript）：同上

这两类挪到 v0.5.3 或更晚，需要决策"独立 CLI binary 还是扩展 audio CLI"。

### dogfood polish

具体内容由 dogfood 数据定，**TBD by data**。候选清单（按可能性排）：

- **HallucinationFilter 调参**：如果 `hallucination_review.py` 显示 filter 拦掉的段里 ≥ 20% 是真实有效语音 → 阈值/规则需要松
- **Live drain 卡顿修**：如果 `live_drain.py` 显示 30s 录音的 drain time p95 > 500ms → 调查瓶颈（可能是 maxTokens=448 在长段被打满，或 Metal 队列堆）
- **Capsule overlay 阈值调整**：v0.5.1 dogfood 已经发现 540s 警告窗口实测难触发。如果用户感受是"想测又测不到"，加 Settings 一个 dev-only override
- **IME 切换体感**：如果 CJK 用户报 IME 闪烁明显，研究 segment-injection 时的 IME save/restore 是否每段都做（应该可以缓存）
- **Focus drop 体感**：如果 `focus_drop.py` 显示 > 5% 段被丢，调查 frontmost detection 是否有 race
- **dl_init 修法 ship 验证**：v0.5.1 ship 后用户重测 cached prepare 时长，如果还是 > 2s 说明 `ModelStore.isComplete` 检查比 upstream `weightsExist` 严，需要对齐

## 核心任务

### Live mode UI 公开

- [ ] `Sources/VoiceTyping/Menu/SettingsWindow.swift` — `ModelsTab` 改造：
  - "Streaming (experimental)" SectionCard 改名 "Streaming"
  - 内部加第二个 Toggle "Live transcription (experimental)" binding `$state.liveStreamingEnabled`
  - 第一个 Toggle 文案改 "Post-record streaming" / "Recording ends, then VAD segments transcribe"
  - 第二个 Toggle 文案 "Live transcription (experimental)" / "Segments appear in the focused app while you talk; refine not supported"
  - 两个 Toggle 都 `.disabled(!state.asrBackend.isQwen)` + 文案 "Only Qwen backends support streaming"
- [ ] 单元测试：UserDefaults round-trip for `liveStreamingEnabled` （应该已经有 `streamingEnabled` 等的覆盖，照样补一个）
- [ ] **不做**: 移除 `defaults write` 隐藏开关的能力。两套并存：UI toggle 是主路径，UserDefaults key 仍然能直接写（毕竟就是 backing store）
- [ ] devlog v0.5.2.md 记录改名前后对照表，方便用户从 dogfood 期间的隐藏 toggle 过渡到 UI

### 分析脚本

- [ ] `Scripts/analysis/summary.py` — sessions/audio/segments/injections + per-backend split
- [ ] `Scripts/analysis/hallucination_review.py` — filter ± 详情 + 可选 `--sample N`
- [ ] `Scripts/analysis/live_drain.py` — Fn↑ → 最后 inject 完成的延迟分布
- [ ] `Scripts/analysis/focus_drop.py` — focusChanged 比例 + 按 target bundleID 分组
- [ ] `Scripts/analysis/README.md` — 一段使用方法 + 每个脚本一段解释 + 几个常见调用范例
- [ ] `docs/debug-captures.md` 末尾加 "## 进阶分析" 段链到 `Scripts/analysis/README.md`
- [ ] **不做** Swift CLI binary（`vt-analyze`）。Python 脚本够 v0.5.2 使用；Swift binary 等下个版本看真实需求

### dogfood polish (TBD by data)

dogfood 数据进来 + 用户判断启动后这块 fill in。当前空。

### 测试

- [ ] `AppStateTests` 加 `testLiveStreamingEnabled_RoundTrips` （如果文件不存在就新建）
- [ ] Python 脚本各自加 `__main__` 守卫 + 一个 `test_*.py` 跑 stdlib `unittest` 验 happy path（可选；脚本简单到不一定值得）
- [ ] dogfood 期间手动验：开 UI toggle → 行为同之前 `defaults write`；关 UI toggle → 行为同之前 `defaults write false`

## 本版本不做

- ❌ **VAD auto-stop (hands-free 模式)** —— 推到 v0.5.3 单独成版。需要专门的 UX 设计：tap-vs-hold 检测、模式状态 UI 指示、abort 手势、和 Fn-hold 模式共存的心智模型
- ❌ **Live + refine 组合** —— refine 专版统一处理（Cmd+Z chain across N segments 太脆，需要 spec 三种方案对比）
- ❌ **Refine I/O capture** —— 同上，绑在 refine 专版
- ❌ **VAD config replay** + **跨 backend ASR 对比** —— 需要 Swift 跑模型，独立 Swift CLI 设计
- ❌ **Debug capture Swift CLI binary (`vt-analyze`)** —— Python 脚本够用；如果 v0.5.2 dogfood 期间发现需要跨 session SQLite query 再立项
- ❌ **WhisperKit live 路径** —— Whisper 没有 streaming engine，沿用 v0.5.0 限制
- ❌ **快捷键可配置 / 历史转录** —— v0.6+ backlog

## 风险与未知

- **dogfood polish 内容空**：本 doc 起草时 v0.5.1 ship 还没满 24 小时，dogfood 数据几乎为零。"polish" 段需要真实数据填实，否则 v0.5.2 实际会变成"只做 UI 公开 + 脚本"。这是预期状态——polish 是 placeholder，不是漏写
- **Live UI 公开会拉高 dogfood pool**：之前只有显式 `defaults write` 的用户在用 live mode；UI 公开后默认入口存在，dogfood 数据量会增加，但也会引入"以前没碰过 live 的用户首次接触"的反馈噪声（vs 老用户的 informed feedback）
- **`Post-record streaming` 改名 vs 老用户**：现有 `streamingEnabled = true` 的老用户在 v0.5.2 升级后看到的是 "Post-record streaming" 而非 "VAD-segmented transcription"。功能未变，只是名字换了。如果担心混淆，devlog v0.5.2.md 显式说明
- **Python stdlib only 的限制**：`live_drain.py` 算 percentile / histogram 需要手写小算法（statistics.quantiles 在 3.8+ 可用，足够）。如果以后想画图必须升级到 matplotlib，那时就破"零依赖"承诺
- **分析脚本面对的实际场景未知**：首批 4 个里可能有不用的也可能缺的。v0.5.2 末尾 retrospective 决定第二批方向（是不是该把 VAD replay 提上来？是不是该加 `inject_latency.py`？）
- **`liveStreamingEnabled` 老用户**：v0.5.2 ship 后老用户看到 toggle 已经是 ON 状态（因为 UserDefaults 有值），不需要二次操作。但如果他们之前 `defaults write false` 过又忘了，会奇怪 "Live transcription" 默认是关的。文案不强调默认值，只说功能行为，避免误导

## v0.5.3 advancement criteria

dogfood 数据攒到用户认可"够"才启动 v0.5.3。无硬性 5 天 / 30 Fn 指标（v0.5.1.md 里写的那套指标继续作为参考，不作为 gate）。建议在启动 v0.5.3 之前用本版本的分析脚本至少跑一遍：

- [ ] `summary.py` 跑一遍知道用 live 多深
- [ ] `hallucination_review.py` 抽样 20 个被拦段，人工判断里面真实语音 vs 训练尾巴比例
- [ ] `live_drain.py` 跑一遍看 p95 drain time 分布是否在可接受范围
- [ ] `focus_drop.py` 跑一遍确认 focus drop 比例 < 5%

如果四项任一明显异常，先发 v0.5.2 polish patch 修对应问题，再 dogfood 一阵，再启动 v0.5.3。
