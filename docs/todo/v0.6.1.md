# v0.6.1 — TBD

_Opened: 2026-04-25_
_Status: scope pending — 用户拍板中_

## 必须

- [ ] _TBD（用户填）_

## 可选

- [ ] _TBD（用户填）_

## 候选池（v0.6.0 系列遗留 / roadmap 短期）

> 用户来挑哪些进 v0.6.1，哪些留 backlog 或推 v0.7.0：

- **`THIRD_PARTY_LICENSES.md`**：v0.6.0 scope doc 显式 punt 到 v0.6.x。Sparkle (MIT) / WhisperKit / speech-swift / Silero VAD / Qwen 模型 各依赖 license attribution
- **CI 自动化发版**：v0.6.0 scope doc 显式 punt 到 v0.6.x patch（GitHub Actions 跑 `make release` + 上传 DMG + push appcast；管 EdDSA 私钥的安全方案待设计）
- **dogfood 信号回填**：`#33` 5+ 天 live mode 真实使用收集 → 阈值校准 / 0.6B echo 调研用得上
- **WhisperKit 下载进度** ([backlog 短期](backlog.md))：当前 indeterminate；看 upstream 能不能给细粒度回调
- **快捷键可配置** ([backlog 短期](backlog.md))：除 Fn 外提供 Right Option / Right Cmd 等替代；Settings 加 hotkey picker
- **历史转录记录** ([backlog 短期](backlog.md))：可选保存最近 N 条转录到 Settings → History tab，支持复制 / 重新发送

## 未排入但记得做的（来自 v0.6.0 系列经验）

- 验证 `#34 ship v0.5.1` 是否还需要——v0.5.x 全 fold 进 v0.6.0 后这个 task 可能 obsolete，由用户在拍 scope 时一并 close
