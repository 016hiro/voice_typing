# v0.7.1 — TBD

> **状态**：skeleton 2026-05-02。v0.7.0 ship 后 5 天 dogfood 反馈再排主线工作。
>
> **来源**：v0.7.0 close 时主线无 carryover；具体未决项已迁 [`backlog.md`](backlog.md) "v0.7.0 close 迁入" 段。
>
> v0.7.0 carryover 待 dogfood 信号触发后再从 backlog 提升到本版本：
>
> - `#R10` integration tests — 流式异常路径 mocks 化（mid-stream cloud↔local 切换 / 网络断开 / mlx OOM）
> - `#R11` 16 GB Mac dogfood —— 跟 v0.6.3 那条合并；新 `LocalLiveSegmentSession` 跨段 KV cache 累积可能更早触发 swap
> - `replaceLastInjection` IME bypass —— live cloud 路径 Cmd+Z + CJK IME 拦截 bug，dogfood 报告 → 修

## 目标

待 v0.7.0 dogfood 信号后填。

## TODO

- [ ] 整理 v0.7.1 scope（dogfood 5 天后）

## 收尾

- [ ] Info.plist bump
- [ ] devlog
- [ ] CHANGELOG
- [ ] make release + gh release
