# v0.7.2 — Per-app hotwords

> **状态**：scope skeleton 2026-05-02，2026-05-07 更新——v0.7.1 advancement criteria 已在 v0.7.1 自身周期内完成（#B7 pin 56 sessions dogfood，≥5s 率 5.3%→0%），本版本不再背 v0.7.1 验证任务。
>
> **来源**：v0.7.1 close 时唯一 carryover 是 #B5（per-app hotwords），用户拍板单独占版本。

## 主线 #B5：Per-app 热词配置

> **状态**：scope 待 `/think` 拍板，本身阻塞 schema 决策。

- 现状：`ContextProfile` 只携带 `systemPromptSnippet`；dictionary `state.dictionary.entries` 是全局
- 候选 schema：
  - **(a)** `ContextProfile.dictionaryFilter: [UUID]?` 引用全局 dict 子集（白名单）。global dict 仍是 superset
  - **(b)** 完全独立 `PerAppDictionaryStore`，profile 自带 entries
  - **(c)** `ContextProfile.dictionaryMode: enum { all / whitelist / blacklist }` + 配套 `[UUID]`
- UI 改动：profile editor sheet 加多选列表，从全局 dict 列出 checkbox
- pipeline 触点：`stopRecording` 路径上 `GlossaryBuilder` 的 entries 输入按当前 profile 过滤

## TODO

- [ ] `/think` 拍板 #B5 schema (a/b/c)
- [ ] `/think` 拍板 UI 设计（profile editor sheet 多选列表样式 / sub-table / popover ?）
- [ ] 实现 schema migration（pre-v0.7.2 ContextProfile JSON → 新 schema）
- [ ] pipeline 触点改造（GlossaryBuilder entries 过滤）
- [ ] Settings UI 多选列表
- [ ] tests

## 收尾

- [ ] Info.plist bump
- [ ] devlog
- [ ] CHANGELOG
- [ ] make release + gh release
